import ROICalculatorClient from '@/components/sections/ROICalculatorClient'

export const metadata = {
  title: 'ROI-kalkylator — Test',
  robots: { index: false, follow: false },
}

export default function ROITestPage() {
  return <ROICalculatorClient />
}
